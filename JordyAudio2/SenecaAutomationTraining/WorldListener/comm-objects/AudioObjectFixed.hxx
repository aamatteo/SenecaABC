/* ------------------------------------------------------------------ */
/*      item            : AudioObjectFixed.hxx
        generated by    : matteo (matteo)
        date            : Wed 15 Dec 2021
        category        : header file
        description     : DUECA Communication Object (DCO)
                          automatically generated by dueca-codegen
        codegen version : 110
        language        : C++
        item            : AudioObjectFixed.dco
        made by         : Rene' van Paassen
        date            : 171130
        description     : Volume and pitch of an audio object
        copyright       : (c) 2017 TUDelft-AE-C&S
*/

#ifndef AudioObjectFixed_hxx
#define AudioObjectFixed_hxx

#include <inttypes.h>

#if !defined(__DCO_NOPACK)
namespace dueca {
class AmorphStore;
class AmorphReStore;
struct DataWriterArraySize;
};
#endif
#if !defined(__DCO_STANDALONE)
namespace dueca {
struct CommObjectDataTable;
};
#include <gencodegen.h>
#if GENCODEGEN != 110
#error "Generated AudioObjectFixed.hxx too old, please clean with 'make mrproper'"
#endif
#include <CommObjectTraits.hxx>
#endif
#include <iostream>


#ifdef DUECA_CONFIG_HDF5
#include <H5Cpp.h>
#include <hdf5utils/HDF5DCOMetaFunctor.hxx>
#endif


/** This provides basic audio properties of a sound fixed in the world
    for example sounds of a static object. 
   
    sounds in event channels are played as events come in, and played once,
    sounds in stream channels are played continuously looping
    */
struct AudioObjectFixed
{
  /** typedef for internal reference */
  typedef AudioObjectFixed __ThisDCOType__;

public:
  /** The name of this class. */
  static const char* const classname;


  /** volume of the playback */
  float volume;

  /** pitch value of the playback */
  float pitch;

public:
  /** a "magic" number, hashed out of the class definition,
      that will be used to check consistency of the sent objects
      across the dueca nodes. */
  static const uint32_t magic_check_number;

  /** default constructor. */
  AudioObjectFixed();

  /** Constructor with arguments */
  AudioObjectFixed(
        const float& volume,
        const float& pitch);

  /** copy constructor. */
  AudioObjectFixed(const AudioObjectFixed& o);

#if !defined(__DCO_NOPACK)
  /** constructor to restore an AudioObjectFixed from amorphous storage. */
  AudioObjectFixed(dueca::AmorphReStore& r);
#endif

  /** destructor. */
  ~AudioObjectFixed();

#if !defined(__DCO_STANDALONE)
  /** new operator "new", which places objects not on a
      heap, but in one of the memory arenas. This to speed up
      memory management. */
  static void* operator new(size_t size);

  /** new operator "delete", to go with the new version
      of operator new. */
  static void operator delete(void* p);

  /** placement "new", needed for stl. */
  inline static void* operator new(size_t size, AudioObjectFixed*& o)
  { return reinterpret_cast<void*>(o); }
#endif

#if !defined(__DCO_NOPACK)
  /** packs the AudioObjectFixed into amorphous storage. */
  void packData(::dueca::AmorphStore& s) const;

  /** packs the AudioObjectFixed into amorphous storage.
      only differences with a previous object are packed. */
  void packDataDiff(::dueca::AmorphStore& s, const AudioObjectFixed& ref) const;

  /** unpacks the AudioObjectFixed from an amorphous storage. */
  void unPackData(::dueca::AmorphReStore& s);

  /** unpacks the differences for AudioObjectFixed
      from an amorphous storage. */
  void unPackDataDiff(::dueca::AmorphReStore& s);
#endif

  /** Test for equality. */
  bool operator == (const AudioObjectFixed& o) const;

  /** Test for inequality. */
  inline bool operator != (const AudioObjectFixed& o) const
  { return !(*this == o); }

  /** Assignment operator. */
  AudioObjectFixed& operator=(const AudioObjectFixed& o);

  /** prints the AudioObjectFixed to a stream. */
  std::ostream & print(std::ostream& s) const;
};

#if !defined(__DCO_NOPACK)
/** pack the object into amorphous storage. */
inline void packData(::dueca::AmorphStore& s,
                     const AudioObjectFixed& o)
{ o.packData(s); }

/** pack the differences between this object and another
    into amorphous storage. */
inline void packDataDiff(dueca::AmorphStore& s,
                         const AudioObjectFixed& o,
                         const AudioObjectFixed& ref)
{ o.packDataDiff(s, ref); }

/** unpack the object from amorphous storage. */
inline void unPackData(::dueca::AmorphReStore& s,
                       AudioObjectFixed& o)
{ o.unPackData(s); }

/** unpack the differences to this object from storage. */
inline void unPackDataDiff(dueca::AmorphReStore& s,
                           AudioObjectFixed& o)
{ o.unPackDataDiff(s); }
#endif

namespace std {
/** print to a stream. */
inline std::ostream &
operator << (std::ostream& s, const AudioObjectFixed& o)
{ return o.print(s); }
};

#if !defined(__DCO_STANDALONE)
namespace dueca {
/** Template specialization, defines a trait that is needed if
    AudioObjectFixed is ever used inside other dco objects. */
template <>
struct dco_nested<AudioObjectFixed> : public dco_isnested { };
};
#endif

#if defined(DUECA_CONFIG_HDF5)
// functors guarded in a separate namespace
namespace AudioObjectFixed_space {

  // writes to file, reading from channel
  class HDF5DCOWriteFunctor: public dueca::hdf5log::HDF5DCOWriteFunctor
  {
    AudioObjectFixed example;
  public:
    // constructor, to be invoked by the metafunctor
    HDF5DCOWriteFunctor(boost::weak_ptr<H5::H5File> file,
                        const std::string& path,
                        size_t chunksize,
                        const std::string& label,
                        bool compress, bool writeticks,
                        const dueca::DataTimeSpec* startend);

    // the functor member used by channel reading code
    bool operator() (const void* dpointer, const dueca::DataTimeSpec& ts);
  };

  // reads from file, writing to channel
  class HDF5DCOReadFunctor: public dueca::hdf5log::HDF5DCOReadFunctor
  {
    AudioObjectFixed example;
  public:
    // constructor, to be invoked by the metafunctor
    HDF5DCOReadFunctor(boost::weak_ptr<H5::H5File> file,
                       const std::string& path,
                       bool readticks);
    // the functor member used by channel writing code
    bool operator() (void* dpointer);
  };
} // end namespace AudioObjectFixed_space
#endif


#endif
