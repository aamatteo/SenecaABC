/* ------------------------------------------------------------------ */
/*      item            : AutopilotChannel.hxx
        generated by    : matteo (matteo)
        date            : Wed 15 Dec 2021
        category        : header file
        description     : DUECA Communication Object (DCO)
                          automatically generated by dueca-codegen
        codegen version : 110
        language        : C++
*/

#ifndef AutopilotChannel_hxx
#define AutopilotChannel_hxx

#include <inttypes.h>

#if !defined(__DCO_NOPACK)
namespace dueca {
class AmorphStore;
class AmorphReStore;
struct DataWriterArraySize;
};
#endif
#if !defined(__DCO_STANDALONE)
namespace dueca {
struct CommObjectDataTable;
};
#include <gencodegen.h>
#if GENCODEGEN != 110
#error "Generated AutopilotChannel.hxx too old, please clean with 'make mrproper'"
#endif
#include <CommObjectTraits.hxx>
#endif
#include <iostream>




/** An automatically generated class
    */
struct AutopilotChannel
{
  /** typedef for internal reference */
  typedef AutopilotChannel __ThisDCOType__;

public:
  /** The name of this class. */
  static const char* const classname;


  /** A class member */
  int APstate;

  /** A class member */
  float de;

  /** A class member */
  float da;

  /** A class member */
  float dr;

  /** A class member */
  float theta_ref;

  /** A class member */
  float phi_ref;

  /** A class member */
  float needle_deviation;

  /** A class member */
  float dme;

  /** A class member */
  float brg;

  /** A class member */
  float course;

  /** A class member */
  float dtk;

  /** A class member */
  float nav_R;

  /** A class member */
  float vnav_R;

  /** A class member */
  float dist_to_top;

  /** A class member */
  float time_to_bod;

  /** A class member */
  float turn_rate;

  /** A class member */
  float fpa_req;

public:
  /** a "magic" number, hashed out of the class definition,
      that will be used to check consistency of the sent objects
      across the dueca nodes. */
  static const uint32_t magic_check_number;

  /** default constructor. */
  AutopilotChannel();

  /** Constructor with arguments */
  AutopilotChannel(
        const int& APstate,
        const float& de,
        const float& da,
        const float& dr,
        const float& theta_ref,
        const float& phi_ref,
        const float& needle_deviation,
        const float& dme,
        const float& brg,
        const float& course,
        const float& dtk,
        const float& nav_R,
        const float& vnav_R,
        const float& dist_to_top,
        const float& time_to_bod,
        const float& turn_rate,
        const float& fpa_req);

  /** copy constructor. */
  AutopilotChannel(const AutopilotChannel& o);

#if !defined(__DCO_NOPACK)
  /** constructor to restore an AutopilotChannel from amorphous storage. */
  AutopilotChannel(dueca::AmorphReStore& r);
#endif

  /** destructor. */
  ~AutopilotChannel();

#if !defined(__DCO_STANDALONE)
  /** new operator "new", which places objects not on a
      heap, but in one of the memory arenas. This to speed up
      memory management. */
  static void* operator new(size_t size);

  /** new operator "delete", to go with the new version
      of operator new. */
  static void operator delete(void* p);

  /** placement "new", needed for stl. */
  inline static void* operator new(size_t size, AutopilotChannel*& o)
  { return reinterpret_cast<void*>(o); }
#endif

#if !defined(__DCO_NOPACK)
  /** packs the AutopilotChannel into amorphous storage. */
  void packData(::dueca::AmorphStore& s) const;

  /** packs the AutopilotChannel into amorphous storage.
      only differences with a previous object are packed. */
  void packDataDiff(::dueca::AmorphStore& s, const AutopilotChannel& ref) const;

  /** unpacks the AutopilotChannel from an amorphous storage. */
  void unPackData(::dueca::AmorphReStore& s);

  /** unpacks the differences for AutopilotChannel
      from an amorphous storage. */
  void unPackDataDiff(::dueca::AmorphReStore& s);
#endif

  /** Test for equality. */
  bool operator == (const AutopilotChannel& o) const;

  /** Test for inequality. */
  inline bool operator != (const AutopilotChannel& o) const
  { return !(*this == o); }

  /** Assignment operator. */
  AutopilotChannel& operator=(const AutopilotChannel& o);

  /** prints the AutopilotChannel to a stream. */
  std::ostream & print(std::ostream& s) const;
};

#if !defined(__DCO_NOPACK)
/** pack the object into amorphous storage. */
inline void packData(::dueca::AmorphStore& s,
                     const AutopilotChannel& o)
{ o.packData(s); }

/** pack the differences between this object and another
    into amorphous storage. */
inline void packDataDiff(dueca::AmorphStore& s,
                         const AutopilotChannel& o,
                         const AutopilotChannel& ref)
{ o.packDataDiff(s, ref); }

/** unpack the object from amorphous storage. */
inline void unPackData(::dueca::AmorphReStore& s,
                       AutopilotChannel& o)
{ o.unPackData(s); }

/** unpack the differences to this object from storage. */
inline void unPackDataDiff(dueca::AmorphReStore& s,
                           AutopilotChannel& o)
{ o.unPackDataDiff(s); }
#endif

namespace std {
/** print to a stream. */
inline std::ostream &
operator << (std::ostream& s, const AutopilotChannel& o)
{ return o.print(s); }
};

#if !defined(__DCO_STANDALONE)
namespace dueca {
/** Template specialization, defines a trait that is needed if
    AutopilotChannel is ever used inside other dco objects. */
template <>
struct dco_nested<AutopilotChannel> : public dco_isnested { };
};
#endif


#endif
