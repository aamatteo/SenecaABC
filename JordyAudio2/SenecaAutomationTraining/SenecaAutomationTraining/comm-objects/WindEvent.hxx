/* ------------------------------------------------------------------ */
/*      item            : WindEvent.hxx
        generated by    : matteo (matteo)
        date            : Wed 15 Dec 2021
        category        : header file
        description     : DUECA Communication Object (DCO)
                          automatically generated by dueca-codegen
        codegen version : 110
        language        : C++
*/

#ifndef WindEvent_hxx
#define WindEvent_hxx

#include <inttypes.h>

#if !defined(__DCO_NOPACK)
namespace dueca {
class AmorphStore;
class AmorphReStore;
struct DataWriterArraySize;
};
#endif
#if !defined(__DCO_STANDALONE)
namespace dueca {
struct CommObjectDataTable;
};
#include <gencodegen.h>
#if GENCODEGEN != 110
#error "Generated WindEvent.hxx too old, please clean with 'make mrproper'"
#endif
#include <CommObjectTraits.hxx>
#endif
#include <iostream>




/** for wind_vel, wind_dir & visibility, negative values (defaults) are ignored (no update send to FlightGear)
    */
struct WindEvent
{
  /** typedef for internal reference */
  typedef WindEvent __ThisDCOType__;

public:
  /** The name of this class. */
  static const char* const classname;


  /** A class member */
  int id;

  /** A class member */
  bool turbulence;

  /** A class member */
  float turb_int;

  /** A class member */
  float wind_vel;

  /** A class member */
  float wind_dir;

  /** A class member */
  bool gust;

  /** used only for FG
      visibility in meters
      cloud altitude in feet */
  int visibility;

  /** A class member */
  int cloud_layer_0_altitude;

  /** A class member */
  int cloud_layer_1_altitude;

  /** A class member */
  int cloud_layer_2_altitude;

public:
  /** a "magic" number, hashed out of the class definition,
      that will be used to check consistency of the sent objects
      across the dueca nodes. */
  static const uint32_t magic_check_number;

  /** default constructor. */
  WindEvent();

  /** Constructor with arguments */
  WindEvent(
        const int& id,
        const bool& turbulence,
        const float& turb_int,
        const float& wind_vel,
        const float& wind_dir,
        const bool& gust,
        const int& visibility,
        const int& cloud_layer_0_altitude,
        const int& cloud_layer_1_altitude,
        const int& cloud_layer_2_altitude);

  /** copy constructor. */
  WindEvent(const WindEvent& o);

#if !defined(__DCO_NOPACK)
  /** constructor to restore an WindEvent from amorphous storage. */
  WindEvent(dueca::AmorphReStore& r);
#endif

  /** destructor. */
  ~WindEvent();

#if !defined(__DCO_STANDALONE)
  /** new operator "new", which places objects not on a
      heap, but in one of the memory arenas. This to speed up
      memory management. */
  static void* operator new(size_t size);

  /** new operator "delete", to go with the new version
      of operator new. */
  static void operator delete(void* p);

  /** placement "new", needed for stl. */
  inline static void* operator new(size_t size, WindEvent*& o)
  { return reinterpret_cast<void*>(o); }
#endif

#if !defined(__DCO_NOPACK)
  /** packs the WindEvent into amorphous storage. */
  void packData(::dueca::AmorphStore& s) const;

  /** packs the WindEvent into amorphous storage.
      only differences with a previous object are packed. */
  void packDataDiff(::dueca::AmorphStore& s, const WindEvent& ref) const;

  /** unpacks the WindEvent from an amorphous storage. */
  void unPackData(::dueca::AmorphReStore& s);

  /** unpacks the differences for WindEvent
      from an amorphous storage. */
  void unPackDataDiff(::dueca::AmorphReStore& s);
#endif

  /** Test for equality. */
  bool operator == (const WindEvent& o) const;

  /** Test for inequality. */
  inline bool operator != (const WindEvent& o) const
  { return !(*this == o); }

  /** Assignment operator. */
  WindEvent& operator=(const WindEvent& o);

  /** prints the WindEvent to a stream. */
  std::ostream & print(std::ostream& s) const;
};

#if !defined(__DCO_NOPACK)
/** pack the object into amorphous storage. */
inline void packData(::dueca::AmorphStore& s,
                     const WindEvent& o)
{ o.packData(s); }

/** pack the differences between this object and another
    into amorphous storage. */
inline void packDataDiff(dueca::AmorphStore& s,
                         const WindEvent& o,
                         const WindEvent& ref)
{ o.packDataDiff(s, ref); }

/** unpack the object from amorphous storage. */
inline void unPackData(::dueca::AmorphReStore& s,
                       WindEvent& o)
{ o.unPackData(s); }

/** unpack the differences to this object from storage. */
inline void unPackDataDiff(dueca::AmorphReStore& s,
                           WindEvent& o)
{ o.unPackDataDiff(s); }
#endif

namespace std {
/** print to a stream. */
inline std::ostream &
operator << (std::ostream& s, const WindEvent& o)
{ return o.print(s); }
};

#if !defined(__DCO_STANDALONE)
namespace dueca {
/** Template specialization, defines a trait that is needed if
    WindEvent is ever used inside other dco objects. */
template <>
struct dco_nested<WindEvent> : public dco_isnested { };
};
#endif


#endif
