/* ------------------------------------------------------------------ */
/*      item            : MotionActuatorLengths.hxx
        generated by    : matteo (matteo)
        date            : Wed 15 Dec 2021
        category        : header file
        description     : DUECA Communication Object (DCO)
                          automatically generated by dueca-codegen
        codegen version : 110
        language        : C++
*/

#ifndef MotionActuatorLengths_hxx
#define MotionActuatorLengths_hxx

#include <inttypes.h>

#if !defined(__DCO_NOPACK)
namespace dueca {
class AmorphStore;
class AmorphReStore;
struct DataWriterArraySize;
};
#endif
#if !defined(__DCO_STANDALONE)
namespace dueca {
struct CommObjectDataTable;
};
#include <gencodegen.h>
#if GENCODEGEN != 110
#error "Generated MotionActuatorLengths.hxx too old, please clean with 'make mrproper'"
#endif
#include <CommObjectTraits.hxx>
#endif
#include <iostream>

#include <fixvector.hxx>


/**setpoints for actuator lengths (29APR2002: deprecated | 02MAY2002 now used for logging)
   FROM MotionLimiter
   TO MotionController (alternative)
   NOTE (29APR2002: deprecated) status is bitmask with 0x01 as testbit
        no output to motion base when testbit is set!
    */
struct MotionActuatorLengths
{
  /** typedef for internal reference */
  typedef MotionActuatorLengths __ThisDCOType__;

public:
  /** The name of this class. */
  static const char* const classname;


  /** A class member */
  int status;

  /** A class member */
  dueca::fixvector<6,double> j;

  /** A class member */
  dueca::fixvector<6,double> jd;

  /** A class member */
  dueca::fixvector<6,double> jdd;

public:
  /** a "magic" number, hashed out of the class definition,
      that will be used to check consistency of the sent objects
      across the dueca nodes. */
  static const uint32_t magic_check_number;

  /** default constructor. */
  MotionActuatorLengths();

  /** Constructor with arguments */
  MotionActuatorLengths(
        const int& status,
        const dueca::fixvector<6,double>& j,
        const dueca::fixvector<6,double>& jd,
        const dueca::fixvector<6,double>& jdd);

  /** Constructor with all single (non-iterable) arguments */
  MotionActuatorLengths(
        const int& status);

  /** copy constructor. */
  MotionActuatorLengths(const MotionActuatorLengths& o);

#if !defined(__DCO_NOPACK)
  /** constructor to restore an MotionActuatorLengths from amorphous storage. */
  MotionActuatorLengths(dueca::AmorphReStore& r);
#endif

  /** destructor. */
  ~MotionActuatorLengths();

#if !defined(__DCO_STANDALONE)
  /** new operator "new", which places objects not on a
      heap, but in one of the memory arenas. This to speed up
      memory management. */
  static void* operator new(size_t size);

  /** new operator "delete", to go with the new version
      of operator new. */
  static void operator delete(void* p);

  /** placement "new", needed for stl. */
  inline static void* operator new(size_t size, MotionActuatorLengths*& o)
  { return reinterpret_cast<void*>(o); }
#endif

#if !defined(__DCO_NOPACK)
  /** packs the MotionActuatorLengths into amorphous storage. */
  void packData(::dueca::AmorphStore& s) const;

  /** packs the MotionActuatorLengths into amorphous storage.
      only differences with a previous object are packed. */
  void packDataDiff(::dueca::AmorphStore& s, const MotionActuatorLengths& ref) const;

  /** unpacks the MotionActuatorLengths from an amorphous storage. */
  void unPackData(::dueca::AmorphReStore& s);

  /** unpacks the differences for MotionActuatorLengths
      from an amorphous storage. */
  void unPackDataDiff(::dueca::AmorphReStore& s);
#endif

  /** Test for equality. */
  bool operator == (const MotionActuatorLengths& o) const;

  /** Test for inequality. */
  inline bool operator != (const MotionActuatorLengths& o) const
  { return !(*this == o); }

  /** Assignment operator. */
  MotionActuatorLengths& operator=(const MotionActuatorLengths& o);

  /** prints the MotionActuatorLengths to a stream. */
  std::ostream & print(std::ostream& s) const;
};

#if !defined(__DCO_NOPACK)
/** pack the object into amorphous storage. */
inline void packData(::dueca::AmorphStore& s,
                     const MotionActuatorLengths& o)
{ o.packData(s); }

/** pack the differences between this object and another
    into amorphous storage. */
inline void packDataDiff(dueca::AmorphStore& s,
                         const MotionActuatorLengths& o,
                         const MotionActuatorLengths& ref)
{ o.packDataDiff(s, ref); }

/** unpack the object from amorphous storage. */
inline void unPackData(::dueca::AmorphReStore& s,
                       MotionActuatorLengths& o)
{ o.unPackData(s); }

/** unpack the differences to this object from storage. */
inline void unPackDataDiff(dueca::AmorphReStore& s,
                           MotionActuatorLengths& o)
{ o.unPackDataDiff(s); }
#endif

namespace std {
/** print to a stream. */
inline std::ostream &
operator << (std::ostream& s, const MotionActuatorLengths& o)
{ return o.print(s); }
};

#if !defined(__DCO_STANDALONE)
namespace dueca {
/** Template specialization, defines a trait that is needed if
    MotionActuatorLengths is ever used inside other dco objects. */
template <>
struct dco_nested<MotionActuatorLengths> : public dco_isnested { };
};
#endif


#endif
