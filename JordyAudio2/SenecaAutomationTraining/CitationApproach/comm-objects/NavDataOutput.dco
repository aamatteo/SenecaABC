(Type float)
(Type bool)
(Type uint8_t)
(Enum FROMTO uint8_t 
  INACTIVE
  TO
  FROM
)
(EventAndStream NavDataOutput
    ;; [deg]
  (float nav1_dir)
  (FROMTO nav1_fromto)
  (bool nav1_gs_alive)
    ;; [deg]
  (float nav1_hdef)
    ;; [deg]
  (float nav1_vdef)
    ;; [m]
  (float nav1_dme_dist)
    ;; [deg]
  (float nav2_dir)
  (FROMTO nav2_fromto)
    ;; [deg]
  (float nav2_hdef)
    ;; [m]
  (float nav2_dme_dist)
    ;; [deg]
  (float adf_dir)  
)
