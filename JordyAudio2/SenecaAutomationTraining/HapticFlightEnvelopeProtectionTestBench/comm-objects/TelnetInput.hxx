/* ------------------------------------------------------------------ */
/*      item            : TelnetInput.hxx
        generated by    : matteo (matteo)
        date            : Wed 15 Dec 2021
        category        : header file
        description     : DUECA Communication Object (DCO)
                          automatically generated by dueca-codegen
        codegen version : 110
        language        : C++
*/

#ifndef TelnetInput_hxx
#define TelnetInput_hxx

#include <inttypes.h>

#if !defined(__DCO_NOPACK)
namespace dueca {
class AmorphStore;
class AmorphReStore;
struct DataWriterArraySize;
};
#endif
#if !defined(__DCO_STANDALONE)
namespace dueca {
struct CommObjectDataTable;
};
#include <gencodegen.h>
#if GENCODEGEN != 110
#error "Generated TelnetInput.hxx too old, please clean with 'make mrproper'"
#endif
#include <CommObjectTraits.hxx>
#endif
#include <iostream>

#include <stringoptions.h>

#include <fixvector.hxx>
#include <stringoptions.h>
typedef dueca::fixvector< 20, vstring > CommandVector;


/** An automatically generated class
    */
struct TelnetInput
{
  /** typedef for internal reference */
  typedef TelnetInput __ThisDCOType__;

public:
  /** The name of this class. */
  static const char* const classname;


  /** This one doesnt work for now  ( Option hdf5 )
      The command is only used when you put this switch to one.
      This is used to protect from total fools using this */
  bool command_ready;

  /** This actually is the command which you want to send/t */
  vstring command;

  /** As an upgrade, I add a vector for sending a sequence of commands
      Can be useful when using NASAL code over telnet to remove an entire set of properties */
  bool command_vector_ready;

  /** The actual vector of commands */
  CommandVector command_vector;

public:
  /** a "magic" number, hashed out of the class definition,
      that will be used to check consistency of the sent objects
      across the dueca nodes. */
  static const uint32_t magic_check_number;

  /** default constructor. */
  TelnetInput();

  /** Constructor with arguments */
  TelnetInput(
        const bool& command_ready,
        const vstring& command,
        const bool& command_vector_ready,
        const CommandVector& command_vector);

  /** Constructor with all single (non-iterable) arguments */
  TelnetInput(
        const bool& command_ready,
        const vstring& command,
        const bool& command_vector_ready);

  /** copy constructor. */
  TelnetInput(const TelnetInput& o);

#if !defined(__DCO_NOPACK)
  /** constructor to restore an TelnetInput from amorphous storage. */
  TelnetInput(dueca::AmorphReStore& r);
#endif

  /** destructor. */
  ~TelnetInput();

#if !defined(__DCO_STANDALONE)
  /** new operator "new", which places objects not on a
      heap, but in one of the memory arenas. This to speed up
      memory management. */
  static void* operator new(size_t size);

  /** new operator "delete", to go with the new version
      of operator new. */
  static void operator delete(void* p);

  /** placement "new", needed for stl. */
  inline static void* operator new(size_t size, TelnetInput*& o)
  { return reinterpret_cast<void*>(o); }
#endif

#if !defined(__DCO_NOPACK)
  /** packs the TelnetInput into amorphous storage. */
  void packData(::dueca::AmorphStore& s) const;

  /** packs the TelnetInput into amorphous storage.
      only differences with a previous object are packed. */
  void packDataDiff(::dueca::AmorphStore& s, const TelnetInput& ref) const;

  /** unpacks the TelnetInput from an amorphous storage. */
  void unPackData(::dueca::AmorphReStore& s);

  /** unpacks the differences for TelnetInput
      from an amorphous storage. */
  void unPackDataDiff(::dueca::AmorphReStore& s);
#endif

  /** Test for equality. */
  bool operator == (const TelnetInput& o) const;

  /** Test for inequality. */
  inline bool operator != (const TelnetInput& o) const
  { return !(*this == o); }

  /** Assignment operator. */
  TelnetInput& operator=(const TelnetInput& o);

  /** prints the TelnetInput to a stream. */
  std::ostream & print(std::ostream& s) const;
};

#if !defined(__DCO_NOPACK)
/** pack the object into amorphous storage. */
inline void packData(::dueca::AmorphStore& s,
                     const TelnetInput& o)
{ o.packData(s); }

/** pack the differences between this object and another
    into amorphous storage. */
inline void packDataDiff(dueca::AmorphStore& s,
                         const TelnetInput& o,
                         const TelnetInput& ref)
{ o.packDataDiff(s, ref); }

/** unpack the object from amorphous storage. */
inline void unPackData(::dueca::AmorphReStore& s,
                       TelnetInput& o)
{ o.unPackData(s); }

/** unpack the differences to this object from storage. */
inline void unPackDataDiff(dueca::AmorphReStore& s,
                           TelnetInput& o)
{ o.unPackDataDiff(s); }
#endif

namespace std {
/** print to a stream. */
inline std::ostream &
operator << (std::ostream& s, const TelnetInput& o)
{ return o.print(s); }
};

#if !defined(__DCO_STANDALONE)
namespace dueca {
/** Template specialization, defines a trait that is needed if
    TelnetInput is ever used inside other dco objects. */
template <>
struct dco_nested<TelnetInput> : public dco_isnested { };
};
#endif


#endif
