/* ------------------------------------------------------------------ */
/*      item            : StickSwitches.hxx
        generated by    : matteo (matteo)
        date            : Wed 15 Dec 2021
        category        : header file
        description     : DUECA Communication Object (DCO)
                          automatically generated by dueca-codegen
        codegen version : 110
        language        : C++
*/

#ifndef StickSwitches_hxx
#define StickSwitches_hxx

#include <inttypes.h>

#if !defined(__DCO_NOPACK)
namespace dueca {
class AmorphStore;
class AmorphReStore;
struct DataWriterArraySize;
};
#endif
#if !defined(__DCO_STANDALONE)
namespace dueca {
struct CommObjectDataTable;
};
#include <gencodegen.h>
#if GENCODEGEN != 110
#error "Generated StickSwitches.hxx too old, please clean with 'make mrproper'"
#endif
#include <CommObjectTraits.hxx>
#endif
#include <iostream>




/** An automatically generated class
    */
struct StickSwitches
{
  /** typedef for internal reference */
  typedef StickSwitches __ThisDCOType__;

public:
  /** The name of this class. */
  static const char* const classname;


  /** A class member */
  bool trigger;

  /** A class member */
  bool red;

  /** A class member */
  bool black;

  /** A class member */
  bool blue;

  /** A class member */
  bool green;

  /** A class member */
  bool coolie_down;

  /** A class member */
  bool coolie_up;

  /** A class member */
  bool coolie_right;

  /** A class member */
  bool coolie_left;

public:
  /** a "magic" number, hashed out of the class definition,
      that will be used to check consistency of the sent objects
      across the dueca nodes. */
  static const uint32_t magic_check_number;

  /** default constructor. */
  StickSwitches();

  /** Constructor with arguments */
  StickSwitches(
        const bool& trigger,
        const bool& red,
        const bool& black,
        const bool& blue,
        const bool& green,
        const bool& coolie_down,
        const bool& coolie_up,
        const bool& coolie_right,
        const bool& coolie_left);

  /** copy constructor. */
  StickSwitches(const StickSwitches& o);

#if !defined(__DCO_NOPACK)
  /** constructor to restore an StickSwitches from amorphous storage. */
  StickSwitches(dueca::AmorphReStore& r);
#endif

  /** destructor. */
  ~StickSwitches();

#if !defined(__DCO_STANDALONE)
  /** new operator "new", which places objects not on a
      heap, but in one of the memory arenas. This to speed up
      memory management. */
  static void* operator new(size_t size);

  /** new operator "delete", to go with the new version
      of operator new. */
  static void operator delete(void* p);

  /** placement "new", needed for stl. */
  inline static void* operator new(size_t size, StickSwitches*& o)
  { return reinterpret_cast<void*>(o); }
#endif

#if !defined(__DCO_NOPACK)
  /** packs the StickSwitches into amorphous storage. */
  void packData(::dueca::AmorphStore& s) const;

  /** packs the StickSwitches into amorphous storage.
      only differences with a previous object are packed. */
  void packDataDiff(::dueca::AmorphStore& s, const StickSwitches& ref) const;

  /** unpacks the StickSwitches from an amorphous storage. */
  void unPackData(::dueca::AmorphReStore& s);

  /** unpacks the differences for StickSwitches
      from an amorphous storage. */
  void unPackDataDiff(::dueca::AmorphReStore& s);
#endif

  /** Test for equality. */
  bool operator == (const StickSwitches& o) const;

  /** Test for inequality. */
  inline bool operator != (const StickSwitches& o) const
  { return !(*this == o); }

  /** Assignment operator. */
  StickSwitches& operator=(const StickSwitches& o);

  /** prints the StickSwitches to a stream. */
  std::ostream & print(std::ostream& s) const;
};

#if !defined(__DCO_NOPACK)
/** pack the object into amorphous storage. */
inline void packData(::dueca::AmorphStore& s,
                     const StickSwitches& o)
{ o.packData(s); }

/** pack the differences between this object and another
    into amorphous storage. */
inline void packDataDiff(dueca::AmorphStore& s,
                         const StickSwitches& o,
                         const StickSwitches& ref)
{ o.packDataDiff(s, ref); }

/** unpack the object from amorphous storage. */
inline void unPackData(::dueca::AmorphReStore& s,
                       StickSwitches& o)
{ o.unPackData(s); }

/** unpack the differences to this object from storage. */
inline void unPackDataDiff(dueca::AmorphReStore& s,
                           StickSwitches& o)
{ o.unPackDataDiff(s); }
#endif

namespace std {
/** print to a stream. */
inline std::ostream &
operator << (std::ostream& s, const StickSwitches& o)
{ return o.print(s); }
};

#if !defined(__DCO_STANDALONE)
namespace dueca {
/** Template specialization, defines a trait that is needed if
    StickSwitches is ever used inside other dco objects. */
template <>
struct dco_nested<StickSwitches> : public dco_isnested { };
};
#endif


#endif
