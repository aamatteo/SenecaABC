/* ------------------------------------------------------------------ */
/*      item            : SnapshotRequest.hxx
        generated by    : matteo (matteo)
        date            : Thu 24 Mar 2022
        category        : header file
        description     : DUECA Communication Object (DCO)
                          automatically generated by dueca-codegen
        codegen version : 110
        language        : C++
*/

#ifndef SnapshotRequest_hxx
#define SnapshotRequest_hxx

#include <inttypes.h>

#if !defined(__DCO_NOPACK)
namespace dueca {
class AmorphStore;
class AmorphReStore;
struct DataWriterArraySize;
};
#endif
#if !defined(__DCO_STANDALONE)
namespace dueca {
struct CommObjectDataTable;
};
#include <gencodegen.h>
#if GENCODEGEN != 110
#error "Generated SnapshotRequest.hxx too old, please clean with 'make mrproper'"
#endif
#include <CommObjectTraits.hxx>
#endif
#include <iostream>




/** An automatically generated class
    */
struct SnapshotRequest
{
  /** typedef for internal reference */
  typedef SnapshotRequest __ThisDCOType__;

public:
  /** The name of this class. */
  static const char* const classname;


  /** A class member */
  bool makesnapshot;

public:
  /** a "magic" number, hashed out of the class definition,
      that will be used to check consistency of the sent objects
      across the dueca nodes. */
  static const uint32_t magic_check_number;

  /** default constructor. */
  SnapshotRequest();

  /** Constructor with arguments */
  SnapshotRequest(
        const bool& makesnapshot);

  /** copy constructor. */
  SnapshotRequest(const SnapshotRequest& o);

#if !defined(__DCO_NOPACK)
  /** constructor to restore an SnapshotRequest from amorphous storage. */
  SnapshotRequest(dueca::AmorphReStore& r);
#endif

  /** destructor. */
  ~SnapshotRequest();

#if !defined(__DCO_STANDALONE)
  /** new operator "new", which places objects not on a
      heap, but in one of the memory arenas. This to speed up
      memory management. */
  static void* operator new(size_t size);

  /** new operator "delete", to go with the new version
      of operator new. */
  static void operator delete(void* p);

  /** placement "new", needed for stl. */
  inline static void* operator new(size_t size, SnapshotRequest*& o)
  { return reinterpret_cast<void*>(o); }
#endif

#if !defined(__DCO_NOPACK)
  /** packs the SnapshotRequest into amorphous storage. */
  void packData(::dueca::AmorphStore& s) const;

  /** packs the SnapshotRequest into amorphous storage.
      only differences with a previous object are packed. */
  void packDataDiff(::dueca::AmorphStore& s, const SnapshotRequest& ref) const;

  /** unpacks the SnapshotRequest from an amorphous storage. */
  void unPackData(::dueca::AmorphReStore& s);

  /** unpacks the differences for SnapshotRequest
      from an amorphous storage. */
  void unPackDataDiff(::dueca::AmorphReStore& s);
#endif

  /** Test for equality. */
  bool operator == (const SnapshotRequest& o) const;

  /** Test for inequality. */
  inline bool operator != (const SnapshotRequest& o) const
  { return !(*this == o); }

  /** Assignment operator. */
  SnapshotRequest& operator=(const SnapshotRequest& o);

  /** prints the SnapshotRequest to a stream. */
  std::ostream & print(std::ostream& s) const;
};

#if !defined(__DCO_NOPACK)
/** pack the object into amorphous storage. */
inline void packData(::dueca::AmorphStore& s,
                     const SnapshotRequest& o)
{ o.packData(s); }

/** pack the differences between this object and another
    into amorphous storage. */
inline void packDataDiff(dueca::AmorphStore& s,
                         const SnapshotRequest& o,
                         const SnapshotRequest& ref)
{ o.packDataDiff(s, ref); }

/** unpack the object from amorphous storage. */
inline void unPackData(::dueca::AmorphReStore& s,
                       SnapshotRequest& o)
{ o.unPackData(s); }

/** unpack the differences to this object from storage. */
inline void unPackDataDiff(dueca::AmorphReStore& s,
                           SnapshotRequest& o)
{ o.unPackDataDiff(s); }
#endif

namespace std {
/** print to a stream. */
inline std::ostream &
operator << (std::ostream& s, const SnapshotRequest& o)
{ return o.print(s); }
};

#if !defined(__DCO_STANDALONE)
namespace dueca {
/** Template specialization, defines a trait that is needed if
    SnapshotRequest is ever used inside other dco objects. */
template <>
struct dco_nested<SnapshotRequest> : public dco_isnested { };
};
#endif


#endif
