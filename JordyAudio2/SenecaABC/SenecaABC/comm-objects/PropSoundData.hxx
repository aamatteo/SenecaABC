/* ------------------------------------------------------------------ */
/*      item            : PropSoundData.hxx
        generated by    : matteo (matteo)
        date            : Thu 24 Mar 2022
        category        : header file
        description     : DUECA Communication Object (DCO)
                          automatically generated by dueca-codegen
        codegen version : 110
        language        : C++
*/

#ifndef PropSoundData_hxx
#define PropSoundData_hxx

#include <inttypes.h>

#if !defined(__DCO_NOPACK)
namespace dueca {
class AmorphStore;
class AmorphReStore;
struct DataWriterArraySize;
};
#endif
#if !defined(__DCO_STANDALONE)
namespace dueca {
struct CommObjectDataTable;
};
#include <gencodegen.h>
#if GENCODEGEN != 110
#error "Generated PropSoundData.hxx too old, please clean with 'make mrproper'"
#endif
#include <CommObjectTraits.hxx>
#endif
#include <iostream>

#include <fixvector.hxx>
#include <fixvector.hxx>


/** data for piston engine sound generation
    set for 2 engines  0=left 1=right
    power:  engine on/off
    sounds: combine events into a bitmask
    */
struct PropSoundData
{
  /** typedef for internal reference */
  typedef PropSoundData __ThisDCOType__;

public:
  /** The name of this class. */
  static const char* const classname;


  /** A class member */
  dueca::fixvector<2,float> rpm;

  /** A class member */
  dueca::fixvector<2,float> torque;

  /** A class member */
  dueca::fixvector<2,bool> power;

  /** A class member */
  float tas;

  /** A class member */
  int sounds;

public:
  /** a "magic" number, hashed out of the class definition,
      that will be used to check consistency of the sent objects
      across the dueca nodes. */
  static const uint32_t magic_check_number;

  /** default constructor. */
  PropSoundData();

  /** Constructor with arguments */
  PropSoundData(
        const dueca::fixvector<2,float>& rpm,
        const dueca::fixvector<2,float>& torque,
        const dueca::fixvector<2,bool>& power,
        const float& tas,
        const int& sounds);

  /** Constructor with all single (non-iterable) arguments */
  PropSoundData(
        const float& tas,
        const int& sounds);

  /** copy constructor. */
  PropSoundData(const PropSoundData& o);

#if !defined(__DCO_NOPACK)
  /** constructor to restore an PropSoundData from amorphous storage. */
  PropSoundData(dueca::AmorphReStore& r);
#endif

  /** destructor. */
  ~PropSoundData();

#if !defined(__DCO_STANDALONE)
  /** new operator "new", which places objects not on a
      heap, but in one of the memory arenas. This to speed up
      memory management. */
  static void* operator new(size_t size);

  /** new operator "delete", to go with the new version
      of operator new. */
  static void operator delete(void* p);

  /** placement "new", needed for stl. */
  inline static void* operator new(size_t size, PropSoundData*& o)
  { return reinterpret_cast<void*>(o); }
#endif

#if !defined(__DCO_NOPACK)
  /** packs the PropSoundData into amorphous storage. */
  void packData(::dueca::AmorphStore& s) const;

  /** packs the PropSoundData into amorphous storage.
      only differences with a previous object are packed. */
  void packDataDiff(::dueca::AmorphStore& s, const PropSoundData& ref) const;

  /** unpacks the PropSoundData from an amorphous storage. */
  void unPackData(::dueca::AmorphReStore& s);

  /** unpacks the differences for PropSoundData
      from an amorphous storage. */
  void unPackDataDiff(::dueca::AmorphReStore& s);
#endif

  /** Test for equality. */
  bool operator == (const PropSoundData& o) const;

  /** Test for inequality. */
  inline bool operator != (const PropSoundData& o) const
  { return !(*this == o); }

  /** Assignment operator. */
  PropSoundData& operator=(const PropSoundData& o);

  /** prints the PropSoundData to a stream. */
  std::ostream & print(std::ostream& s) const;
};

#if !defined(__DCO_NOPACK)
/** pack the object into amorphous storage. */
inline void packData(::dueca::AmorphStore& s,
                     const PropSoundData& o)
{ o.packData(s); }

/** pack the differences between this object and another
    into amorphous storage. */
inline void packDataDiff(dueca::AmorphStore& s,
                         const PropSoundData& o,
                         const PropSoundData& ref)
{ o.packDataDiff(s, ref); }

/** unpack the object from amorphous storage. */
inline void unPackData(::dueca::AmorphReStore& s,
                       PropSoundData& o)
{ o.unPackData(s); }

/** unpack the differences to this object from storage. */
inline void unPackDataDiff(dueca::AmorphReStore& s,
                           PropSoundData& o)
{ o.unPackDataDiff(s); }
#endif

namespace std {
/** print to a stream. */
inline std::ostream &
operator << (std::ostream& s, const PropSoundData& o)
{ return o.print(s); }
};

#if !defined(__DCO_STANDALONE)
namespace dueca {
/** Template specialization, defines a trait that is needed if
    PropSoundData is ever used inside other dco objects. */
template <>
struct dco_nested<PropSoundData> : public dco_isnested { };
};
#endif


#endif
