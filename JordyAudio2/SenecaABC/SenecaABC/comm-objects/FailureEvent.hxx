/* ------------------------------------------------------------------ */
/*      item            : FailureEvent.hxx
        generated by    : matteo (matteo)
        date            : Thu 24 Mar 2022
        category        : header file
        description     : DUECA Communication Object (DCO)
                          automatically generated by dueca-codegen
        codegen version : 110
        language        : C++
*/

#ifndef FailureEvent_hxx
#define FailureEvent_hxx

#include <inttypes.h>

#if !defined(__DCO_NOPACK)
namespace dueca {
class AmorphStore;
class AmorphReStore;
struct DataWriterArraySize;
};
#endif
#if !defined(__DCO_STANDALONE)
namespace dueca {
struct CommObjectDataTable;
};
#include <gencodegen.h>
#if GENCODEGEN != 110
#error "Generated FailureEvent.hxx too old, please clean with 'make mrproper'"
#endif
#include <CommObjectTraits.hxx>
#endif
#include <iostream>




/** An automatically generated class
    */
struct FailureEvent
{
  /** typedef for internal reference */
  typedef FailureEvent __ThisDCOType__;

public:
  /** The name of this class. */
  static const char* const classname;


  /** A class member */
  bool GPS_failure;

  /** A class member */
  bool VOR_failure;

  /** A class member */
  bool ADC_failure;

  /** A class member */
  bool PFD_failure;

  /** A class member */
  bool GMU_failure;

  /** A class member */
  bool HSI_failure;

  /** A class member */
  bool AFCS_pit;

  /** A class member */
  bool AFCS_rol;

  /** A class member */
  bool clogged_sp;

  /** A class member */
  bool clogged_pt;

public:
  /** a "magic" number, hashed out of the class definition,
      that will be used to check consistency of the sent objects
      across the dueca nodes. */
  static const uint32_t magic_check_number;

  /** default constructor. */
  FailureEvent();

  /** Constructor with arguments */
  FailureEvent(
        const bool& GPS_failure,
        const bool& VOR_failure,
        const bool& ADC_failure,
        const bool& PFD_failure,
        const bool& GMU_failure,
        const bool& HSI_failure,
        const bool& AFCS_pit,
        const bool& AFCS_rol,
        const bool& clogged_sp,
        const bool& clogged_pt);

  /** copy constructor. */
  FailureEvent(const FailureEvent& o);

#if !defined(__DCO_NOPACK)
  /** constructor to restore an FailureEvent from amorphous storage. */
  FailureEvent(dueca::AmorphReStore& r);
#endif

  /** destructor. */
  ~FailureEvent();

#if !defined(__DCO_STANDALONE)
  /** new operator "new", which places objects not on a
      heap, but in one of the memory arenas. This to speed up
      memory management. */
  static void* operator new(size_t size);

  /** new operator "delete", to go with the new version
      of operator new. */
  static void operator delete(void* p);

  /** placement "new", needed for stl. */
  inline static void* operator new(size_t size, FailureEvent*& o)
  { return reinterpret_cast<void*>(o); }
#endif

#if !defined(__DCO_NOPACK)
  /** packs the FailureEvent into amorphous storage. */
  void packData(::dueca::AmorphStore& s) const;

  /** packs the FailureEvent into amorphous storage.
      only differences with a previous object are packed. */
  void packDataDiff(::dueca::AmorphStore& s, const FailureEvent& ref) const;

  /** unpacks the FailureEvent from an amorphous storage. */
  void unPackData(::dueca::AmorphReStore& s);

  /** unpacks the differences for FailureEvent
      from an amorphous storage. */
  void unPackDataDiff(::dueca::AmorphReStore& s);
#endif

  /** Test for equality. */
  bool operator == (const FailureEvent& o) const;

  /** Test for inequality. */
  inline bool operator != (const FailureEvent& o) const
  { return !(*this == o); }

  /** Assignment operator. */
  FailureEvent& operator=(const FailureEvent& o);

  /** prints the FailureEvent to a stream. */
  std::ostream & print(std::ostream& s) const;
};

#if !defined(__DCO_NOPACK)
/** pack the object into amorphous storage. */
inline void packData(::dueca::AmorphStore& s,
                     const FailureEvent& o)
{ o.packData(s); }

/** pack the differences between this object and another
    into amorphous storage. */
inline void packDataDiff(dueca::AmorphStore& s,
                         const FailureEvent& o,
                         const FailureEvent& ref)
{ o.packDataDiff(s, ref); }

/** unpack the object from amorphous storage. */
inline void unPackData(::dueca::AmorphReStore& s,
                       FailureEvent& o)
{ o.unPackData(s); }

/** unpack the differences to this object from storage. */
inline void unPackDataDiff(dueca::AmorphReStore& s,
                           FailureEvent& o)
{ o.unPackDataDiff(s); }
#endif

namespace std {
/** print to a stream. */
inline std::ostream &
operator << (std::ostream& s, const FailureEvent& o)
{ return o.print(s); }
};

#if !defined(__DCO_STANDALONE)
namespace dueca {
/** Template specialization, defines a trait that is needed if
    FailureEvent is ever used inside other dco objects. */
template <>
struct dco_nested<FailureEvent> : public dco_isnested { };
};
#endif


#endif
