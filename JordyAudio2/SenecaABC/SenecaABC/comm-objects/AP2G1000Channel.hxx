/* ------------------------------------------------------------------ */
/*      item            : AP2G1000Channel.hxx
        generated by    : matteo (matteo)
        date            : Thu 24 Mar 2022
        category        : header file
        description     : DUECA Communication Object (DCO)
                          automatically generated by dueca-codegen
        codegen version : 110
        language        : C++
*/

#ifndef AP2G1000Channel_hxx
#define AP2G1000Channel_hxx

#include <inttypes.h>

#if !defined(__DCO_NOPACK)
namespace dueca {
class AmorphStore;
class AmorphReStore;
struct DataWriterArraySize;
};
#endif
#if !defined(__DCO_STANDALONE)
namespace dueca {
struct CommObjectDataTable;
};
#include <gencodegen.h>
#if GENCODEGEN != 110
#error "Generated AP2G1000Channel.hxx too old, please clean with 'make mrproper'"
#endif
#include <CommObjectTraits.hxx>
#endif
#include <iostream>




/** An automatically generated class
    */
struct AP2G1000Channel
{
  /** typedef for internal reference */
  typedef AP2G1000Channel __ThisDCOType__;

public:
  /** The name of this class. */
  static const char* const classname;


  /** A class member */
  int APstate;

  /** A class member */
  int ap_vertical;

  /** A class member */
  int ap_lateral;

  /** A class member */
  int ap_armed;

  /** A class member */
  float target_alt;

  /** A class member */
  float target_speed;

  /** A class member */
  float hdg_bug;

  /** A class member */
  float target_vs;

  /** A class member */
  float alt_hold;

  /** A class member */
  bool ap_flash;

  /** A class member */
  bool pit_flash;

  /** A class member */
  bool rol_flash;

  /** A class member */
  int active_nav_source;

  /** A class member */
  float course;

  /** A class member */
  int vnav_situation;

  /** A class member */
  float altitude_target_vnav;

  /** A class member */
  int active_leg;

  /** A class member */
  int active_vnv_leg;

  /** A class member */
  int alts_flash_state;

  /** A class member */
  int alt_flash_state;

  /** A class member */
  bool vor_flag_state;

public:
  /** a "magic" number, hashed out of the class definition,
      that will be used to check consistency of the sent objects
      across the dueca nodes. */
  static const uint32_t magic_check_number;

  /** default constructor. */
  AP2G1000Channel();

  /** Constructor with arguments */
  AP2G1000Channel(
        const int& APstate,
        const int& ap_vertical,
        const int& ap_lateral,
        const int& ap_armed,
        const float& target_alt,
        const float& target_speed,
        const float& hdg_bug,
        const float& target_vs,
        const float& alt_hold,
        const bool& ap_flash,
        const bool& pit_flash,
        const bool& rol_flash,
        const int& active_nav_source,
        const float& course,
        const int& vnav_situation,
        const float& altitude_target_vnav,
        const int& active_leg,
        const int& active_vnv_leg,
        const int& alts_flash_state,
        const int& alt_flash_state,
        const bool& vor_flag_state);

  /** copy constructor. */
  AP2G1000Channel(const AP2G1000Channel& o);

#if !defined(__DCO_NOPACK)
  /** constructor to restore an AP2G1000Channel from amorphous storage. */
  AP2G1000Channel(dueca::AmorphReStore& r);
#endif

  /** destructor. */
  ~AP2G1000Channel();

#if !defined(__DCO_STANDALONE)
  /** new operator "new", which places objects not on a
      heap, but in one of the memory arenas. This to speed up
      memory management. */
  static void* operator new(size_t size);

  /** new operator "delete", to go with the new version
      of operator new. */
  static void operator delete(void* p);

  /** placement "new", needed for stl. */
  inline static void* operator new(size_t size, AP2G1000Channel*& o)
  { return reinterpret_cast<void*>(o); }
#endif

#if !defined(__DCO_NOPACK)
  /** packs the AP2G1000Channel into amorphous storage. */
  void packData(::dueca::AmorphStore& s) const;

  /** packs the AP2G1000Channel into amorphous storage.
      only differences with a previous object are packed. */
  void packDataDiff(::dueca::AmorphStore& s, const AP2G1000Channel& ref) const;

  /** unpacks the AP2G1000Channel from an amorphous storage. */
  void unPackData(::dueca::AmorphReStore& s);

  /** unpacks the differences for AP2G1000Channel
      from an amorphous storage. */
  void unPackDataDiff(::dueca::AmorphReStore& s);
#endif

  /** Test for equality. */
  bool operator == (const AP2G1000Channel& o) const;

  /** Test for inequality. */
  inline bool operator != (const AP2G1000Channel& o) const
  { return !(*this == o); }

  /** Assignment operator. */
  AP2G1000Channel& operator=(const AP2G1000Channel& o);

  /** prints the AP2G1000Channel to a stream. */
  std::ostream & print(std::ostream& s) const;
};

#if !defined(__DCO_NOPACK)
/** pack the object into amorphous storage. */
inline void packData(::dueca::AmorphStore& s,
                     const AP2G1000Channel& o)
{ o.packData(s); }

/** pack the differences between this object and another
    into amorphous storage. */
inline void packDataDiff(dueca::AmorphStore& s,
                         const AP2G1000Channel& o,
                         const AP2G1000Channel& ref)
{ o.packDataDiff(s, ref); }

/** unpack the object from amorphous storage. */
inline void unPackData(::dueca::AmorphReStore& s,
                       AP2G1000Channel& o)
{ o.unPackData(s); }

/** unpack the differences to this object from storage. */
inline void unPackDataDiff(dueca::AmorphReStore& s,
                           AP2G1000Channel& o)
{ o.unPackDataDiff(s); }
#endif

namespace std {
/** print to a stream. */
inline std::ostream &
operator << (std::ostream& s, const AP2G1000Channel& o)
{ return o.print(s); }
};

#if !defined(__DCO_STANDALONE)
namespace dueca {
/** Template specialization, defines a trait that is needed if
    AP2G1000Channel is ever used inside other dco objects. */
template <>
struct dco_nested<AP2G1000Channel> : public dco_isnested { };
};
#endif


#endif
