/* ------------------------------------------------------------------ */
/*      item            : GFC700Event.hxx
        generated by    : matteo (matteo)
        date            : Thu 24 Mar 2022
        category        : header file
        description     : DUECA Communication Object (DCO)
                          automatically generated by dueca-codegen
        codegen version : 110
        language        : C++
*/

#ifndef GFC700Event_hxx
#define GFC700Event_hxx

#include <inttypes.h>

#if !defined(__DCO_NOPACK)
namespace dueca {
class AmorphStore;
class AmorphReStore;
struct DataWriterArraySize;
};
#endif
#if !defined(__DCO_STANDALONE)
namespace dueca {
struct CommObjectDataTable;
};
#include <gencodegen.h>
#if GENCODEGEN != 110
#error "Generated GFC700Event.hxx too old, please clean with 'make mrproper'"
#endif
#include <CommObjectTraits.hxx>
#endif
#include <iostream>




/** An automatically generated class
    */
struct GFC700Event
{
  /** typedef for internal reference */
  typedef GFC700Event __ThisDCOType__;

public:
  /** The name of this class. */
  static const char* const classname;


  /** Enumerated type for an automatically generated object class */
  enum msgType {
    GFC700_AP,
    GFC700_VS,
    GFC700_ALT,
    GFC700_VNV,
    GFC700_FLC,
    GFC700_HDG,
    GFC700_NAV,
    GFC700_FD,
    GFC700_APR,
    GFC700_BC,
    GFC700_NOSEUP,
    GFC700_NOSEDN,
    GFC700_CDI,
    GFC700_NONE,
    GFC700_FPL,
    GFC700_DME,
    GFC700_SYNC
  };

  /** A class member */
  msgType msg;

  /** A class member */
  float alt_select;

  /** A class member */
  float hdg_select;

  /** A class member */
  float crs_select;

  /** A class member */
  int range;

public:
  /** a "magic" number, hashed out of the class definition,
      that will be used to check consistency of the sent objects
      across the dueca nodes. */
  static const uint32_t magic_check_number;

  /** default constructor. */
  GFC700Event();

  /** Constructor with arguments */
  GFC700Event(
        const msgType& msg,
        const float& alt_select,
        const float& hdg_select,
        const float& crs_select,
        const int& range);

  /** copy constructor. */
  GFC700Event(const GFC700Event& o);

#if !defined(__DCO_NOPACK)
  /** constructor to restore an GFC700Event from amorphous storage. */
  GFC700Event(dueca::AmorphReStore& r);
#endif

  /** destructor. */
  ~GFC700Event();

#if !defined(__DCO_STANDALONE)
  /** new operator "new", which places objects not on a
      heap, but in one of the memory arenas. This to speed up
      memory management. */
  static void* operator new(size_t size);

  /** new operator "delete", to go with the new version
      of operator new. */
  static void operator delete(void* p);

  /** placement "new", needed for stl. */
  inline static void* operator new(size_t size, GFC700Event*& o)
  { return reinterpret_cast<void*>(o); }
#endif

#if !defined(__DCO_NOPACK)
  /** packs the GFC700Event into amorphous storage. */
  void packData(::dueca::AmorphStore& s) const;

  /** packs the GFC700Event into amorphous storage.
      only differences with a previous object are packed. */
  void packDataDiff(::dueca::AmorphStore& s, const GFC700Event& ref) const;

  /** unpacks the GFC700Event from an amorphous storage. */
  void unPackData(::dueca::AmorphReStore& s);

  /** unpacks the differences for GFC700Event
      from an amorphous storage. */
  void unPackDataDiff(::dueca::AmorphReStore& s);
#endif

  /** Test for equality. */
  bool operator == (const GFC700Event& o) const;

  /** Test for inequality. */
  inline bool operator != (const GFC700Event& o) const
  { return !(*this == o); }

  /** Assignment operator. */
  GFC700Event& operator=(const GFC700Event& o);

  /** prints the GFC700Event to a stream. */
  std::ostream & print(std::ostream& s) const;
};

#if !defined(__DCO_NOPACK)
/** pack the object into amorphous storage. */
inline void packData(::dueca::AmorphStore& s,
                     const GFC700Event& o)
{ o.packData(s); }

/** pack the differences between this object and another
    into amorphous storage. */
inline void packDataDiff(dueca::AmorphStore& s,
                         const GFC700Event& o,
                         const GFC700Event& ref)
{ o.packDataDiff(s, ref); }

/** unpack the object from amorphous storage. */
inline void unPackData(::dueca::AmorphReStore& s,
                       GFC700Event& o)
{ o.unPackData(s); }

/** unpack the differences to this object from storage. */
inline void unPackDataDiff(dueca::AmorphReStore& s,
                           GFC700Event& o)
{ o.unPackDataDiff(s); }
#endif

const char* const getString(const GFC700Event::msgType &o);
void readFromString(GFC700Event::msgType &o, const std::string& s);
#if !defined(__DCO_NOPACK)
void packData(::dueca::AmorphStore& s,
              const GFC700Event::msgType &o);
void unPackData(::dueca::AmorphReStore& s,
                GFC700Event::msgType &o);
#endif
namespace dueca {
  template <>
  const char* getclassname<GFC700Event::msgType>();
};

PRINT_NS_START;
inline std::ostream&
operator << (std::ostream& s, const GFC700Event::msgType& o)
{ return s << getString(o); }
inline std::istream&
operator >> (std::istream& s, GFC700Event::msgType& o)
{ std::string tmp; s >> tmp; readFromString(o, tmp); return s; }
PRINT_NS_END;

namespace std {
/** print to a stream. */
inline std::ostream &
operator << (std::ostream& s, const GFC700Event& o)
{ return o.print(s); }
};

#if !defined(__DCO_STANDALONE)
namespace dueca {
/** Template specialization, defines a trait that is needed if
    GFC700Event is ever used inside other dco objects. */
template <>
struct dco_nested<GFC700Event> : public dco_isnested { };
template <>
struct dco_nested< GFC700Event::msgType > : public dco_isenum { };
};
#endif


#endif
