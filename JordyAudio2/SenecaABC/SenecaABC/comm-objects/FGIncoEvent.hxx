/* ------------------------------------------------------------------ */
/*      item            : FGIncoEvent.hxx
        generated by    : matteo (matteo)
        date            : Thu 24 Mar 2022
        category        : header file
        description     : DUECA Communication Object (DCO)
                          automatically generated by dueca-codegen
        codegen version : 110
        language        : C++
*/

#ifndef FGIncoEvent_hxx
#define FGIncoEvent_hxx

#include <inttypes.h>

#if !defined(__DCO_NOPACK)
namespace dueca {
class AmorphStore;
class AmorphReStore;
struct DataWriterArraySize;
};
#endif
#if !defined(__DCO_STANDALONE)
namespace dueca {
struct CommObjectDataTable;
};
#include <gencodegen.h>
#if GENCODEGEN != 110
#error "Generated FGIncoEvent.hxx too old, please clean with 'make mrproper'"
#endif
#include <CommObjectTraits.hxx>
#endif
#include <iostream>




/** An automatically generated class
    */
struct FGIncoEvent
{
  /** typedef for internal reference */
  typedef FGIncoEvent __ThisDCOType__;

public:
  /** The name of this class. */
  static const char* const classname;


  /** A class member */
  float lat_zero;

  /** A class member */
  float lon_zero;

  /** A class member */
  float alt_zero;

  /** A class member */
  float psi_zero;

public:
  /** a "magic" number, hashed out of the class definition,
      that will be used to check consistency of the sent objects
      across the dueca nodes. */
  static const uint32_t magic_check_number;

  /** default constructor. */
  FGIncoEvent();

  /** Constructor with arguments */
  FGIncoEvent(
        const float& lat_zero,
        const float& lon_zero,
        const float& alt_zero,
        const float& psi_zero);

  /** copy constructor. */
  FGIncoEvent(const FGIncoEvent& o);

#if !defined(__DCO_NOPACK)
  /** constructor to restore an FGIncoEvent from amorphous storage. */
  FGIncoEvent(dueca::AmorphReStore& r);
#endif

  /** destructor. */
  ~FGIncoEvent();

#if !defined(__DCO_STANDALONE)
  /** new operator "new", which places objects not on a
      heap, but in one of the memory arenas. This to speed up
      memory management. */
  static void* operator new(size_t size);

  /** new operator "delete", to go with the new version
      of operator new. */
  static void operator delete(void* p);

  /** placement "new", needed for stl. */
  inline static void* operator new(size_t size, FGIncoEvent*& o)
  { return reinterpret_cast<void*>(o); }
#endif

#if !defined(__DCO_NOPACK)
  /** packs the FGIncoEvent into amorphous storage. */
  void packData(::dueca::AmorphStore& s) const;

  /** packs the FGIncoEvent into amorphous storage.
      only differences with a previous object are packed. */
  void packDataDiff(::dueca::AmorphStore& s, const FGIncoEvent& ref) const;

  /** unpacks the FGIncoEvent from an amorphous storage. */
  void unPackData(::dueca::AmorphReStore& s);

  /** unpacks the differences for FGIncoEvent
      from an amorphous storage. */
  void unPackDataDiff(::dueca::AmorphReStore& s);
#endif

  /** Test for equality. */
  bool operator == (const FGIncoEvent& o) const;

  /** Test for inequality. */
  inline bool operator != (const FGIncoEvent& o) const
  { return !(*this == o); }

  /** Assignment operator. */
  FGIncoEvent& operator=(const FGIncoEvent& o);

  /** prints the FGIncoEvent to a stream. */
  std::ostream & print(std::ostream& s) const;
};

#if !defined(__DCO_NOPACK)
/** pack the object into amorphous storage. */
inline void packData(::dueca::AmorphStore& s,
                     const FGIncoEvent& o)
{ o.packData(s); }

/** pack the differences between this object and another
    into amorphous storage. */
inline void packDataDiff(dueca::AmorphStore& s,
                         const FGIncoEvent& o,
                         const FGIncoEvent& ref)
{ o.packDataDiff(s, ref); }

/** unpack the object from amorphous storage. */
inline void unPackData(::dueca::AmorphReStore& s,
                       FGIncoEvent& o)
{ o.unPackData(s); }

/** unpack the differences to this object from storage. */
inline void unPackDataDiff(dueca::AmorphReStore& s,
                           FGIncoEvent& o)
{ o.unPackDataDiff(s); }
#endif

namespace std {
/** print to a stream. */
inline std::ostream &
operator << (std::ostream& s, const FGIncoEvent& o)
{ return o.print(s); }
};

#if !defined(__DCO_STANDALONE)
namespace dueca {
/** Template specialization, defines a trait that is needed if
    FGIncoEvent is ever used inside other dco objects. */
template <>
struct dco_nested<FGIncoEvent> : public dco_isnested { };
};
#endif


#endif
