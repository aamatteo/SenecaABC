/* ------------------------------------------------------------------ */
/*      item            : SideStickInput.hxx
        generated by    : matteo (matteo)
        date            : Thu 24 Mar 2022
        category        : header file
        description     : DUECA Communication Object (DCO)
                          automatically generated by dueca-codegen
        codegen version : 110
        language        : C++
*/

#ifndef SideStickInput_hxx
#define SideStickInput_hxx

#include <inttypes.h>

#if !defined(__DCO_NOPACK)
namespace dueca {
class AmorphStore;
class AmorphReStore;
struct DataWriterArraySize;
};
#endif
#if !defined(__DCO_STANDALONE)
namespace dueca {
struct CommObjectDataTable;
};
#include <gencodegen.h>
#if GENCODEGEN != 110
#error "Generated SideStickInput.hxx too old, please clean with 'make mrproper'"
#endif
#include <CommObjectTraits.hxx>
#endif
#include <iostream>

#include "SideStickInputParams.hxx"


/** An automatically generated class
    */
struct SideStickInput
{
  /** typedef for internal reference */
  typedef SideStickInput __ThisDCOType__;

public:
  /** The name of this class. */
  static const char* const classname;


  /** pitch channel */
  int use_pitch_params;

  /** A class member */
  float pitch_friction;

  /** A class member */
  float pitch_jam_pos;

  /** A class member */
  float pitch_pos_stop;

  /** A class member */
  float pitch_neg_stop;

  /** A class member */
  float pitch_mass;

  /** A class member */
  float pitch_damping;

  /** A class member */
  float pitch_force;

  /** A class member */
  float pitch_switch;

  /** A class member */
  float pitch_trim_vel;

  /** A class member */
  float pitch_init_trim_pos;

  /** roll channel */
  int use_roll_params;

  /** A class member */
  float roll_friction;

  /** A class member */
  float roll_jam_pos;

  /** A class member */
  float roll_pos_stop;

  /** A class member */
  float roll_neg_stop;

  /** A class member */
  float roll_mass;

  /** A class member */
  float roll_damping;

  /** A class member */
  float roll_force;

  /** A class member */
  float roll_switch;

  /** A class member */
  float roll_trim_vel;

  /** A class member */
  float roll_init_trim_pos;

public:
  /** a "magic" number, hashed out of the class definition,
      that will be used to check consistency of the sent objects
      across the dueca nodes. */
  static const uint32_t magic_check_number;

  /** default constructor. */
  SideStickInput();

  /** Constructor with arguments */
  SideStickInput(
        const int& use_pitch_params,
        const float& pitch_friction,
        const float& pitch_jam_pos,
        const float& pitch_pos_stop,
        const float& pitch_neg_stop,
        const float& pitch_mass,
        const float& pitch_damping,
        const float& pitch_force,
        const float& pitch_switch,
        const float& pitch_trim_vel,
        const float& pitch_init_trim_pos,
        const int& use_roll_params,
        const float& roll_friction,
        const float& roll_jam_pos,
        const float& roll_pos_stop,
        const float& roll_neg_stop,
        const float& roll_mass,
        const float& roll_damping,
        const float& roll_force,
        const float& roll_switch,
        const float& roll_trim_vel,
        const float& roll_init_trim_pos);

  /** copy constructor. */
  SideStickInput(const SideStickInput& o);

#if !defined(__DCO_NOPACK)
  /** constructor to restore an SideStickInput from amorphous storage. */
  SideStickInput(dueca::AmorphReStore& r);
#endif

  /** destructor. */
  ~SideStickInput();

#if !defined(__DCO_STANDALONE)
  /** new operator "new", which places objects not on a
      heap, but in one of the memory arenas. This to speed up
      memory management. */
  static void* operator new(size_t size);

  /** new operator "delete", to go with the new version
      of operator new. */
  static void operator delete(void* p);

  /** placement "new", needed for stl. */
  inline static void* operator new(size_t size, SideStickInput*& o)
  { return reinterpret_cast<void*>(o); }
#endif

#if !defined(__DCO_NOPACK)
  /** packs the SideStickInput into amorphous storage. */
  void packData(::dueca::AmorphStore& s) const;

  /** packs the SideStickInput into amorphous storage.
      only differences with a previous object are packed. */
  void packDataDiff(::dueca::AmorphStore& s, const SideStickInput& ref) const;

  /** unpacks the SideStickInput from an amorphous storage. */
  void unPackData(::dueca::AmorphReStore& s);

  /** unpacks the differences for SideStickInput
      from an amorphous storage. */
  void unPackDataDiff(::dueca::AmorphReStore& s);
#endif

  /** Test for equality. */
  bool operator == (const SideStickInput& o) const;

  /** Test for inequality. */
  inline bool operator != (const SideStickInput& o) const
  { return !(*this == o); }

  /** Assignment operator. */
  SideStickInput& operator=(const SideStickInput& o);

  /** prints the SideStickInput to a stream. */
  std::ostream & print(std::ostream& s) const;
};

#if !defined(__DCO_NOPACK)
/** pack the object into amorphous storage. */
inline void packData(::dueca::AmorphStore& s,
                     const SideStickInput& o)
{ o.packData(s); }

/** pack the differences between this object and another
    into amorphous storage. */
inline void packDataDiff(dueca::AmorphStore& s,
                         const SideStickInput& o,
                         const SideStickInput& ref)
{ o.packDataDiff(s, ref); }

/** unpack the object from amorphous storage. */
inline void unPackData(::dueca::AmorphReStore& s,
                       SideStickInput& o)
{ o.unPackData(s); }

/** unpack the differences to this object from storage. */
inline void unPackDataDiff(dueca::AmorphReStore& s,
                           SideStickInput& o)
{ o.unPackDataDiff(s); }
#endif

namespace std {
/** print to a stream. */
inline std::ostream &
operator << (std::ostream& s, const SideStickInput& o)
{ return o.print(s); }
};

#if !defined(__DCO_STANDALONE)
namespace dueca {
/** Template specialization, defines a trait that is needed if
    SideStickInput is ever used inside other dco objects. */
template <>
struct dco_nested<SideStickInput> : public dco_isnested { };
};
#endif


#endif
