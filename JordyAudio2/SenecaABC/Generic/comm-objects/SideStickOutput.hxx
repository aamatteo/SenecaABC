/* ------------------------------------------------------------------ */
/*      item            : SideStickOutput.hxx
        generated by    : matteo (matteo)
        date            : Thu 24 Mar 2022
        category        : header file
        description     : DUECA Communication Object (DCO)
                          automatically generated by dueca-codegen
        codegen version : 110
        language        : C++
*/

#ifndef SideStickOutput_hxx
#define SideStickOutput_hxx

#include <inttypes.h>

#if !defined(__DCO_NOPACK)
namespace dueca {
class AmorphStore;
class AmorphReStore;
struct DataWriterArraySize;
};
#endif
#if !defined(__DCO_STANDALONE)
namespace dueca {
struct CommObjectDataTable;
};
#include <gencodegen.h>
#if GENCODEGEN != 110
#error "Generated SideStickOutput.hxx too old, please clean with 'make mrproper'"
#endif
#include <CommObjectTraits.hxx>
#endif
#include <iostream>




/** An automatically generated class
    */
struct SideStickOutput
{
  /** typedef for internal reference */
  typedef SideStickOutput __ThisDCOType__;

public:
  /** The name of this class. */
  static const char* const classname;


  /** pitch channel */
  float pitch_fwd_pos;

  /** A class member */
  float pitch_fwd_vel;

  /** A class member */
  float pitch_act_force;

  /** A class member */
  float pitch_cable_force;

  /** A class member */
  float pitch_trim_pos;

  /** A class member */
  float pitch_aft_pos;

  /** A class member */
  float pitch_act_pos;

  /** roll channel */
  float roll_fwd_pos;

  /** A class member */
  float roll_fwd_vel;

  /** A class member */
  float roll_act_force;

  /** A class member */
  float roll_cable_force;

  /** A class member */
  float roll_trim_pos;

  /** A class member */
  float roll_aft_pos;

  /** A class member */
  float roll_act_pos;

public:
  /** a "magic" number, hashed out of the class definition,
      that will be used to check consistency of the sent objects
      across the dueca nodes. */
  static const uint32_t magic_check_number;

  /** default constructor. */
  SideStickOutput();

  /** Constructor with arguments */
  SideStickOutput(
        const float& pitch_fwd_pos,
        const float& pitch_fwd_vel,
        const float& pitch_act_force,
        const float& pitch_cable_force,
        const float& pitch_trim_pos,
        const float& pitch_aft_pos,
        const float& pitch_act_pos,
        const float& roll_fwd_pos,
        const float& roll_fwd_vel,
        const float& roll_act_force,
        const float& roll_cable_force,
        const float& roll_trim_pos,
        const float& roll_aft_pos,
        const float& roll_act_pos);

  /** copy constructor. */
  SideStickOutput(const SideStickOutput& o);

#if !defined(__DCO_NOPACK)
  /** constructor to restore an SideStickOutput from amorphous storage. */
  SideStickOutput(dueca::AmorphReStore& r);
#endif

  /** destructor. */
  ~SideStickOutput();

#if !defined(__DCO_STANDALONE)
  /** new operator "new", which places objects not on a
      heap, but in one of the memory arenas. This to speed up
      memory management. */
  static void* operator new(size_t size);

  /** new operator "delete", to go with the new version
      of operator new. */
  static void operator delete(void* p);

  /** placement "new", needed for stl. */
  inline static void* operator new(size_t size, SideStickOutput*& o)
  { return reinterpret_cast<void*>(o); }
#endif

#if !defined(__DCO_NOPACK)
  /** packs the SideStickOutput into amorphous storage. */
  void packData(::dueca::AmorphStore& s) const;

  /** packs the SideStickOutput into amorphous storage.
      only differences with a previous object are packed. */
  void packDataDiff(::dueca::AmorphStore& s, const SideStickOutput& ref) const;

  /** unpacks the SideStickOutput from an amorphous storage. */
  void unPackData(::dueca::AmorphReStore& s);

  /** unpacks the differences for SideStickOutput
      from an amorphous storage. */
  void unPackDataDiff(::dueca::AmorphReStore& s);
#endif

  /** Test for equality. */
  bool operator == (const SideStickOutput& o) const;

  /** Test for inequality. */
  inline bool operator != (const SideStickOutput& o) const
  { return !(*this == o); }

  /** Assignment operator. */
  SideStickOutput& operator=(const SideStickOutput& o);

  /** prints the SideStickOutput to a stream. */
  std::ostream & print(std::ostream& s) const;
};

#if !defined(__DCO_NOPACK)
/** pack the object into amorphous storage. */
inline void packData(::dueca::AmorphStore& s,
                     const SideStickOutput& o)
{ o.packData(s); }

/** pack the differences between this object and another
    into amorphous storage. */
inline void packDataDiff(dueca::AmorphStore& s,
                         const SideStickOutput& o,
                         const SideStickOutput& ref)
{ o.packDataDiff(s, ref); }

/** unpack the object from amorphous storage. */
inline void unPackData(::dueca::AmorphReStore& s,
                       SideStickOutput& o)
{ o.unPackData(s); }

/** unpack the differences to this object from storage. */
inline void unPackDataDiff(dueca::AmorphReStore& s,
                           SideStickOutput& o)
{ o.unPackDataDiff(s); }
#endif

namespace std {
/** print to a stream. */
inline std::ostream &
operator << (std::ostream& s, const SideStickOutput& o)
{ return o.print(s); }
};

#if !defined(__DCO_STANDALONE)
namespace dueca {
/** Template specialization, defines a trait that is needed if
    SideStickOutput is ever used inside other dco objects. */
template <>
struct dco_nested<SideStickOutput> : public dco_isnested { };
};
#endif


#endif
